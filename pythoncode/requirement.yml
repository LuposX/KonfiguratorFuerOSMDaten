# can be installed with:
# conda env create --file requirement.yml
name: PSE
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - jupyterlab
  - pyrosm
  - shapely=1.8.4
  - sphinx
  - sphinx-book-theme
  - pip
  - pip:
    - customtkinter